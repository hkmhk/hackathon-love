import reat, { Composant } from 'react'
import styled from '../assets/js/styled'

class Likes extends Component {

    render() {
        return (
            <div></div>
        );
    }
}

export default Likes;
